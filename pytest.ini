[pytest]
; src/ is a flat script directory (no __init__.py, bare imports like
; "import db_jobtools as dbj"), so it needs to be on the import path
; explicitly rather than relying on pytest's rootdir auto-insertion.
pythonpath = src
testpaths = tests
